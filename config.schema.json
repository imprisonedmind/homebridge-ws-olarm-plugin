{
  "pluginAlias": "OlarmWSHomebridgePlugin",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "My Olarm"
      },
      "userEmailPhone": {
        "type": "string",
        "title": "User Email or Phone",
        "description": "The email or phone number used to log in to your Olarm account."
      },
      "userPass": {
        "type": "string",
        "title": "User Password",
        "description": "The password used to log in to your Olarm account."
      }
    }
  }
}
